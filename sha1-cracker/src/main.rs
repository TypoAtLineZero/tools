use std::env;

fn main() {
    let arg: Vec<String> = env::args().collect();

    if args.len() != 3 {
        println!("Usage: ");
        println!(sha1-cracker: <worlist.txt> <sha1_hash>);
        return;
    }
}
